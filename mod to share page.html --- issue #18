<!DOCTYPE html>
<html>
<head>
    <title>Share Data</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <div class="max-w-2xl mx-auto p-4 bg-white shadow mt-6 rounded-lg">

        <!-- Header -->
        <div class="flex justify-between items-center mb-4">
            <h1 class="text-3xl font-extrabold">Meter</h1>
            <div class="space-x-2">
                <button class="bg-black text-white px-3 py-1 rounded">Bill History</button>
                <button class="bg-black text-white px-3 py-1 rounded">Family</button>
                <button class="bg-black text-white px-3 py-1 rounded">My Profile</button>
            </div>
        </div>

        <!-- Report Title -->
        <h2 class="text-sm font-semibold text-gray-700 mb-0.5">January’s Report</h2>

        <!-- Report Preview -->
        <div class="mb-1">
            <img src="{{ url_for('static', filename='images/report-preview.jpg') }}" alt="Report Preview" class="rounded w-full">
        </div>

        <!-- Action Buttons -->
        <div class="flex justify-end gap-2 mb-6">
            <button title="Copy Link" class="bg-black text-white px-3 py-1 rounded hover:bg-gray-800 text-sm">Copy link</button>
            <button title="Email" class="bg-black text-white px-3 py-1 rounded hover:bg-gray-800 text-sm">Email</button>
        </div>

        <!-- Share Section -->
        <h2 class="text-xl font-semibold mb-4">Share</h2>

        {% if shared_to %}
        <div class="mb-4 p-3 bg-green-100 border border-green-300 rounded text-green-800 font-medium">
            ✅ Shared with: {{ shared_to | join(', ') }}
        </div>
        {% endif %}
        
        <form action="/share-data" method="POST" class="space-y-4 mt-6">

            <!-- Family Group -->
            <label class="flex items-center justify-between border-2 border-blue-500 rounded-lg p-4 bg-black text-white cursor-pointer hover:shadow transition">
                <div class="flex items-center">
                    <input type="checkbox" name="share_to" value="family" class="mr-3 w-5 h-5 accent-blue-500">
                    <div>
                        <p class="font-semibold">Family</p>
                        <p class="text-sm text-gray-300">Share to everyone in the household</p>
                    </div>
                </div>
                <div class="flex -space-x-2">
                    <img src="{{ url_for('static', filename='images/avatar1.png') }}" class="w-8 h-8 rounded-full border-2 border-white">
                    <img src="{{ url_for('static', filename='images/avatar2.png') }}" class="w-8 h-8 rounded-full border-2 border-white">
                    <img src="{{ url_for('static', filename='images/avatar3.png') }}" class="w-8 h-8 rounded-full border-2 border-white">
                </div>
            </label>

            <!-- Individual Members -->
            {% for name, avatar in users %}
            <label class="flex items-center justify-between bg-gray-200 rounded-lg p-3">
                <div class="flex items-center">
                    <input type="checkbox" name="share_to" value="{{ name }}" class="mr-3 w-5 h-5">
                    <p class="font-medium">{{ name }}</p>
                </div>
                <img src="{{ url_for('static', filename=avatar) }}" class="w-8 h-8 rounded-full">
            </label>
            {% endfor %}

            <!-- Footer -->
            <div class="flex justify-between items-end mt-10">
                <div class="text-left">
                    <p class="text-xl font-bold">Meter</p>
                    <div class="flex space-x-2 text-gray-600 mt-1 text-sm">
                        <a href="#"><span class="hover:text-black">🌐</span></a>
                        <a href="#"><span class="hover:text-black">💼</span></a>
                        <a href="#"><span class="hover:text-black">▶️</span></a>
                        <a href="#"><span class="hover:text-black">📸</span></a>
                    </div>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="bg-black text-white px-5 py-2 rounded hover:bg-gray-800">
                    Share
                </button>
            </div>
        </form>
    </div>
</body>
</html>
